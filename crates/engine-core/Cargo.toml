[package]
name = "engine-core"
description = "Pure, I/O-free PIM domain model: mail, calendar, contacts, unified people, recipients, identities, and sync/search/write contracts."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
license.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
time = { workspace = true }
idna = { workspace = true }
# `SubmitPayload::RenderedSource` byte fields (rendered MIME is not guaranteed UTF-8).
base64 = { workspace = true }

[lints]
workspace = true
